{
  "name": "@ptcg",
  "version": "0.1.0",
  "description": "Pokemon TCG game simulator",
  "scripts": {
    "start": "node start.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "npm install && npm run build --workspaces"
  },
  "private": true,
  "workspaces": [
    "packages/common",
    "packages/sets",
    "packages/server",
    "packages/play",
    "packages/cordova"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@ptcg/server": "*",
    "@ptcg/sets": "*"
  }
}
